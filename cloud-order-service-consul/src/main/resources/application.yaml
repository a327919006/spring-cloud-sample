server:
  port: 10091

spring:
  application:
    name: ${artifactId}
  cloud:
    consul:
      host: localhost
      port: 8500

management:
  endpoints:
    web:
      exposure:
        include: "*"