spring:
  profiles:
    active: ${profileActive}
  application:
    # 这里的应用名对应 Nacos Config 中的 Data ID，实际应用名称以配置中心的配置为准
    name: ${artifactId}-config
  cloud:
    nacos:
      config:
        # 指定查找名为 ${artifactId}-config.yaml 的配置文件
        file-extension: yaml
      discovery:
        ip: 127.0.0.1
    sentinel:
      transport:
        port: 8720
        dashboard: localhost:8090




